{
  "name": "vic-sms-server",
  "version": "1.0.0",
  "description": "Cloud Run server for automatic SMS sending via Riroschool",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "playwright": "^1.40.0",
    "dotenv": "^16.3.1",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18"
  }
}
